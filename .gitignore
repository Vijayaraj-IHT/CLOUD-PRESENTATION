# This project is plain HTML/CSS/JS — there is nothing to install or build.
# These entries only guard against tooling being reintroduced by accident.
node_modules/
dist/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Logs
*.log
